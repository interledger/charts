{{- include "common.secret" (list . "jopacc-rafiki-middleware.secret") }}
{{- define "jopacc-rafiki-middleware.secret" -}}
{{- $top := first . }}
data:
  {{- range $top.Values.secrets }}
    {{- tpl ($top.Files.Get .) $top | nindent 6 -}}
  {{- end }}
{{- end }}

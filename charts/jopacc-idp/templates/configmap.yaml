{{- include "common.configMap" (list . "jopacc-idp.configMap") }}

{{- define "jopacc-idp.configMap" -}}
{{- $top := first . }}
data:
  {{- range $top.Values.configmap }}
    {{- tpl ($top.Files.Get .) $top | nindent 6 -}}
  {{- end }}
{{- end }}

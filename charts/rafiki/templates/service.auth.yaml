{{- define "rafiki.auth.service" -}}
{{- end }}
{{- if .Values.config.auth.enabled }}
{{- include "common.svc" (list . .Values.services.auth .Values.deployments.auth "rafiki.auth.service") }}
{{- end }}

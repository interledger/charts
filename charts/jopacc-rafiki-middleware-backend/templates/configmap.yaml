{{- include "common.configMap" (list . "jopacc-rafiki-middleware-backend.configMap") }}

{{- define "jopacc-rafiki-middleware-backend.configMap" -}}
{{- $top := first . }}
data:
  {{- range $top.Values.configmap }}
    {{- tpl ($top.Files.Get .) $top | nindent 6 -}}
  {{- end }}
{{- end }}

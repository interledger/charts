{{- include "common.secret" (list . "rafiki-boutique-backend.secret") }}
{{- define "rafiki-boutique-backend.secret" -}}
{{- $top := first . }}
data:
  {{- range $top.Values.secrets }}
    {{- tpl ($top.Files.Get .) $top | nindent 6 -}}
  {{- end }}
{{- end }}
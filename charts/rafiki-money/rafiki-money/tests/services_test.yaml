suite: rafiki-money services - Services
templates:
  - service.backend.yaml
  - service.frontend.yaml
set:
  fullnameOverride: test-rafiki-money
release:
  name: test-rafiki-money
tests:
  - it: 'backend service should be created with correct name'
    template: service.backend.yaml
    asserts:
      - equal:
          path: metadata.name
          value: test-rafiki-money-backend-service
      - equal:
          path: spec.type
          value: ClusterIP
      - equal:
          path: spec.ports[0].port
          value: 4003
      - equal:
          path: spec.ports[0].targetPort
          value: 4003
  - it: 'frontend service should be created with correct name'
    template: service.frontend.yaml
    asserts:
      - equal:
          path: metadata.name
          value: test-rafiki-money-frontend-service
      - equal:
          path: spec.type
          value: ClusterIP
      - equal:
          path: spec.ports[0].port
          value: 4003
      - equal:
          path: spec.ports[0].targetPort
          value: 4003

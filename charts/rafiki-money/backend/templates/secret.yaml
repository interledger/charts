{{- include "common.secret" (list . "rafiki-money-backend.secret") }}

{{- define "rafiki-money-backend.secret" -}}
{{- $top := first . }}
data:
  {{- range $top.Values.secret }}
    {{- tpl ($top.Files.Get .) $top | nindent 6 -}}
  {{- end }}
{{- end }}

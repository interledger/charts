{{- include "common.secret" (list . "interledger-app-backend.secret") }}
{{- define "interledger-app-backend.secret" -}}
{{- $top := first . }}
data:
  {{- range $top.Values.secrets }}
    {{- tpl ($top.Files.Get .) $top | nindent 6 -}}
  {{- end }}
{{- end }}
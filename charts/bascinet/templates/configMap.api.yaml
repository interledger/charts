{{- if .Values.api.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-{{ .Values.api.name }}
  {{- include "common.metadata" (list . .Values.api) | nindent 2 }}
data:
  {{- toYaml (.Values.api.config | default dict) | nindent 2 }}
{{- end }}

suite: Rafiki Backend - Good configs in values
templates:
  - configmap.yaml
release:
  name: rafiki-backend
documentSelector:
  kind: ConfigMap
  apiVersion: v1
  name: rafiki-backend
tests:
  - it: 'NODE_ENV default to production'
    asserts:
      - equal:
          path: data.NODE_ENV
          value: production